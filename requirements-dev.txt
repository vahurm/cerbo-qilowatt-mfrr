# Test / development dependencies for cerbo-qilowatt-mfrr.
#
#   pip install -r agent/requirements.txt -r requirements-dev.txt
#   pytest -q && sh tests/test_grid_setpoint.sh
#
# pytest-cov is optional (coverage reporting); uncomment if desired.
pytest>=8.0,<9.0
# pytest-cov>=5.0
